buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'org.jenkins-ci.tools:gradle-jpi-plugin:0.5.0'
    }
}

apply plugin: 'jpi'
apply plugin: 'idea'

dependencies{
    //Supported by Jenkins core
    groovy (group: 'org.codehaus.groovy', name: 'groovy-all', version: '1.8.9')
}

groupId = 'hudson.plugins.iansPlug'
version = '1.0'
description = 'IanPlugin'

jenkinsPlugin {
    coreVersion = '1.457'
    displayName = 'Totally a display name'
    url = 'http://jfrog.com/'
    developers {
        developer {
            id 'iitow'
            name 'Ian Itow'
            email 'itow0001@gmail.com'
        }
    }
}


task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}
